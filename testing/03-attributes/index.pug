doctype html
html 
    head 
        body 
            input (type="password" name="inpPwd")

            -const myClasses=["class1", "class2"]
            div.my-div(class=myClasses)

            -const myStyles={"color":"red", "background-color":"blue"}
            div(style-myStyles)

            -const myAttributes = {"src":"photo.png", "alt":"thi is photo"}
            img&attributes(myAttributes)

            input(type="text" disabled)